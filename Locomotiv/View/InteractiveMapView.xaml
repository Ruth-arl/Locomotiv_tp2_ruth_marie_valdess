<UserControl x:Class="Locomotiv.View.InteractiveMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Locomotiv.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Colonne gauche: "Carte" (pour l'instant, une liste) -->
        <Grid Grid.Column="0" Margin="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Carte du Réseau" FontSize="32" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- Liste des stations (cliquables) -->
            <GroupBox Grid.Row="1" Header="Stations (cliquez pour voir détails)" Margin="0,0,0,10">
                <ListBox ItemsSource="{Binding Stations}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Nom}"
                                    Command="{Binding DataContext.SelectStationCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding Id}"
                                    Background="#3498DB" Foreground="White"
                                    Padding="15,10" Margin="5"
                                    BorderThickness="0" Cursor="Hand"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>

            <!-- Liste des points d'intérêt -->
            <GroupBox Grid.Row="2" Header="Points d'Intérêt">
                <ListBox ItemsSource="{Binding PointsInteret}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="5">
                                <TextBlock Text="{Binding Nom}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Type}" FontSize="10" Foreground="Gray"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>
        </Grid>

        <!-- Colonne droite: Détails de la station sélectionnée -->
        <Border Grid.Column="1" BorderBrush="#BDC3C7" BorderThickness="1" Padding="15">
            <StackPanel>
                <TextBlock Text="Détails de la Station" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

                <TextBlock Text="{Binding SelectedStation.Nom, FallbackValue='Sélectionnez une station'}"
                           FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

                <TextBlock FontSize="14" Foreground="Gray" Margin="0,0,0,20">
                    <Run Text="Capacité: "/>
                    <Run Text="{Binding TrainsStation.Count}"/>
                    <Run Text=" / "/>
                    <Run Text="{Binding SelectedStation.CapaciteMaximale, FallbackValue='0'}"/>
                </TextBlock>

                <!-- Trains -->
                <TextBlock Text="Trains en gare:" FontSize="16" FontWeight="Bold" Margin="0,0,0,5"/>
                <ListBox ItemsSource="{Binding TrainsStation}" Height="150" Margin="0,0,0,15">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock>
                                    <Run Text="{Binding Numero}" FontWeight="Bold"/>
                                    <Run Text=" - "/>
                                    <Run Text="{Binding Type}"/>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Voies 
                <TextBlock Text="Voies:" FontSize="16" FontWeight="Bold" Margin="0,0,0,5"/>
                <ListBox ItemsSource="{Binding VoiesStation}" Height="100">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="10" Height="10" Margin="0,0,5,0">
                                    <Ellipse.Fill>
                                        <SolidColorBrush Color="{Binding Disponible, Converter={StaticResource BoolToColorConverter}}"/>
                                    </Ellipse.Fill>
                                </Ellipse>
                                <TextBlock Text="{Binding NumeroQuai}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>-->
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
