<UserControl x:Class="Locomotiv.View.StationDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            
        </Grid.RowDefinitions>

        <Button Content="Retour"
                Width="100"
                Height="30"
                HorizontalAlignment="Left"
                Margin="0,0,0,15"
                Command="{Binding RetourCommand}"/>

        <Border BorderBrush="LightGray" BorderThickness="1" Padding="15" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Nom de la station :" FontWeight="Bold"/>
                    <TextBlock Text="{Binding Station.Nom}" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Ville :" FontWeight="Bold"/>
                    <TextBlock Text="{Binding Station.Ville}" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Capacité maximale :" FontWeight="Bold"/>
                    <TextBlock Text="{Binding Station.CapaciteMax}" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Separator Grid.Row="1" Margin="0,15"/>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Padding="10" Margin="5">
                <StackPanel>
                    <TextBlock Text="Trains attribués" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                    <ListBox ItemsSource="{Binding Trains}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{Binding Type}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Etat}" FontStyle="Italic" FontSize="12"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Padding="10" Margin="5">
                <StackPanel>
                    <TextBlock Text="Voies" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                    <ListBox ItemsSource="{Binding Voies}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Nom}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Padding="10" Margin="5">
                <StackPanel>
                    <TextBlock Text="Signaux" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                    <ListBox ItemsSource="{Binding Signaux}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Type}" />
                                    <TextBlock Text="-" />
                                    <TextBlock Text="{Binding Etat}" Foreground="DarkGreen"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Grid.Column="1" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Padding="10" Margin="5">
                <StackPanel>
                    <TextBlock Text="Trains actuellement en gare" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                    <ListBox ItemsSource="{Binding TrainsEnGare}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{Binding Type}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding HeureDepart, StringFormat='Départ : {0:HH:mm}'}"/>
                                    <TextBlock Text="{Binding HeureArrivee, StringFormat='Arrivée : {0:HH:mm}'}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>

        </Grid>
    </Grid>

</UserControl>
